# Initial project idea

### First scope of the project:
    - Plain TCP (no TLS)
    - HTTP/1.1 only
    - Handle GET only
    - One request = one response
    - Text responses only
    - No concurrency at first
    - Blocking and sigle threaded
